function setup() {
  createCanvas(400, 400);
}
function draw() {
  background(75);
  fill(175);
  textSize(75);
  text("G",
    mouseX, mouseY, 200, 200);
  if (mouseIsPressed) {
    noFill();
    stroke(0, 255, 0);
    rect(mouseX, mouseY, 200, 200);
  }
}
